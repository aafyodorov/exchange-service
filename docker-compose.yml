version: "3.7"

services:
  exchange-service:
    build: .
    ports:
    - "8080:8080"
    environment:
      - GIPHY_APP_ID
      - OPEN_EXCH_RATES_APP_ID
    volumes:
      - ./logs_volume:/usr/exchange-service-app/logs